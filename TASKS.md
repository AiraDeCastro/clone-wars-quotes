# TASKS.md

Task breakdown for **Cold Open**, organized by the milestones defined in [docs/PRD.md](docs/PRD.md) §12. Scope, stack, and rationale live in [PLANNING.md](PLANNING.md); guardrails for Claude Code sessions live in [CLAUDE.md](CLAUDE.md).

Check items off as they land. Exit criteria for each milestone is listed at the end of its section — don't call a milestone done until those pass.

---

## Tooling & Quality Gates

- [x] Set up husky pre-commit hook: markdown + corpus lint, corpus test suite, corpus build/validation, `npm audit`
- [x] Set up husky commit-msg hook + commitlint for Conventional Commits
- [x] Write a corpus schema validator ([scripts/validate-corpus.js](scripts/validate-corpus.js)) and its test suite ([tests/corpus.test.js](tests/corpus.test.js))
- [ ] **(newly discovered)** Once the iOS Xcode project exists (below): add SwiftLint + `xcodebuild`/XCTest to the pre-commit hook
- [ ] **(newly discovered)** Once the Android Studio project exists (below): add ktlint + Gradle build/JUnit to the pre-commit hook
- [ ] **(newly discovered)** Once native projects exist: add native dependency vulnerability scanning (CocoaPods/SPM audit, Gradle dependency check) alongside `npm audit`

---

## M1 — Corpus & Core Widget

**Quote corpus**

- [x] Transcribe cold-open narration lines episode-by-episode (season, episode, arc, exact text) — **unblocked:** user supplied a legitimate transcript source ([sagespeculation.com's episode-by-episode compilation](http://www.sagespeculation.com/2017/08/01/star-wars-the-clone-wars-episode-opening-quotes/)); all 129 episodes that have a cold-open line (Seasons 1-7, minus the 4 Season 7 "Siege of Mandalore" finale episodes which genuinely have none) are now in [corpus/quotes.json](corpus/quotes.json). Episode titles cross-checked against Wikipedia's episode list and corrected for a couple of spelling mismatches. `arc` populated per an explicit decision: standalone episodes use their own title, multi-episode arcs (Malevolence, Ryloth, Umbara, Mortis, etc.) identified from Wikipedia's plot synopses, not exhaustive. One conflict with the original single-entry seed (the "Rookies" quote/arc) found and resolved in favor of the new source — see [corpus/README.md](corpus/README.md) for the full explanation.
- [ ] Fact-check transcriptions against source audio/subtitles for accuracy — **partially open:** episode titles are cross-checked against Wikipedia; the quote text itself is sourced from one fan compilation and hasn't been independently verified against the actual episode audio/official captions yet.
- [x] Define the JSON schema: `text`, `episodeTitle`, `season`, `episode`, `arc` — seeded in [corpus/quotes.json](corpus/quotes.json) with one verified entry
- [x] Write the JSON → SQLite build step for on-device querying — `npm run build` now runs [scripts/build-corpus.js](scripts/build-corpus.js), which validates then compiles `corpus/quotes.json` into `corpus/quotes.sqlite` (gitignored, regenerated every build) via Node's built-in `node:sqlite` (no new npm dependency). One `quotes` table with an auto-incrementing `id`, designed so a future client can do `SELECT ... WHERE id NOT IN (shown_ids) ORDER BY RANDOM() LIMIT 1` instead of holding the whole corpus in memory. Covered by [tests/build-corpus.test.js](tests/build-corpus.test.js) (row mapping, sequential ids, idempotent rebuilds, empty-corpus edge case), written before the implementation per CLAUDE.md's commit standards. Verified end-to-end: ran `npm run build` for real and inspected the resulting `.sqlite` file's contents directly.
- [x] Add a validation script that catches schema violations and duplicate entries — [scripts/validate-corpus.js](scripts/validate-corpus.js), covered by [tests/corpus.test.js](tests/corpus.test.js)
- [ ] **(newly discovered)** No platform actually reads `corpus/quotes.sqlite` yet — iOS/Android/Windows all still bundle and parse the flat `corpus/quotes.json` directly (via their respective `sync:*` scripts). Wiring each platform to the compiled SQLite file instead (for the cheap no-repeat query the schema was designed for) is separate follow-up work, not done as part of adding the build step itself.
- [x] Re-verify the "Rookies" (S1E5) cold-open line against the actual episode — the newly-sourced transcript disagreed with the original single-entry seed on both the quote text and its arc; user confirmed the new source ("The best confidence builder is experience.", arc "Rookies") is correct. See [corpus/README.md](corpus/README.md).

**iOS widget**

- [ ] Set up Xcode project + WidgetKit extension target — **source scaffold written** ([ios/](ios/): `project.yml` for XcodeGen, `ColdOpen` app target, `ColdOpenWidgetExtension` target), **but not yet generated, opened, or built** — done from a Windows machine with no Xcode/Swift toolchain available. Next: on macOS, run `xcodegen generate` and confirm it opens and builds in Xcode. See [ios/README.md](ios/README.md).
- [ ] Bundle the compiled corpus with the app target — partially done: `npm run sync:ios` copies `corpus/quotes.json` into the `ColdOpenCore` package resource manually; not yet wired into an Xcode build phase (see newly discovered task below)
- [x] Implement random, no-repeat-until-exhausted quote selection — algorithm implemented and unit-tested in `ColdOpenCore`'s `QuoteSelector` ([ios/ColdOpenCore/Sources/ColdOpenCore/QuoteSelector.swift](ios/ColdOpenCore/Sources/ColdOpenCore/QuoteSelector.swift), tests in `QuoteSelectorTests.swift`), and now wired into `ColdOpenWidget.swift` via `nextQuote()` — actually used by both `getSnapshot` and `getTimeline`, not just tested in isolation. See the persistence task below for the App Group wiring that makes the no-repeat behavior actually stick.
- [ ] Build small and medium widget size layouts (quote only / quote + attribution) — SwiftUI view + `.supportedFamilies([.systemSmall, .systemMedium])` written in `ColdOpenWidget.swift`, not yet visually verified in Simulator
- [x] Wire manual refresh via App Intent + widget timeline reload — `ColdOpenWidget.swift` now declares `RefreshQuoteIntent: AppIntent`, whose `perform()` calls `WidgetCenter.shared.reloadTimelines(ofKind: ColdOpenWidget.kind)` (the actual next-quote draw stays in `nextQuote()`, not duplicated in the intent). Wired to a small `Button(intent:)` with a refresh icon in the attribution row of `ColdOpenWidgetView`. `TimelineProvider`'s reload policy stays `.never` — this intent is what forces a new `getTimeline` call instead. **Unverified by any Swift toolchain** — no `swiftc` here to confirm the `AppIntent` conformance or that widget buttons work as written; this is the standard iOS 17+ interactive-widget pattern, traced by hand against the API shape from training knowledge.
- [ ] Verify WCAG AA contrast on both layouts — can't check without rendering; open
- [x] Persist `QuoteSelector`'s "already shown" state across widget reloads via App Group shared `UserDefaults` — mostly done, same shape as the Android/Windows fixes (mirrored on purpose): `QuoteSelector` gained an `alreadyShown` init param and `shownTexts` property, keyed on `Quote.text`; `ColdOpenWidgetProvider.nextQuote()` reads/writes it via `UserDefaults(suiteName: WidgetAppGroup.identifier)`, falling back to un-persisted random selection if that suite is unavailable (`nil`) rather than crashing. 4 new XCTest cases added, traced by hand (no Swift toolchain here to run `swift test`). **What's genuinely not done, and can't be from here:** the App Group identifier (`group.com.coldopen.app`, shared by both iOS and macOS widget extensions — verified via research that this deliberately avoids needing a `#if os(...)` branch in the shared source) is written into new `.entitlements` files for `ColdOpenWidgetExtension`/`ColdOpenWidgetExtensionMac` and wired via `CODE_SIGN_ENTITLEMENTS` in `project.yml`, but the App Group itself isn't *registered* anywhere — that's an interactive Signing & Capabilities step tied to a real Apple ID, unlike every other fix in this file so far. Researched first (App Group provisioning is genuinely account-tier-dependent, unlike the pure file-format questions resolved elsewhere) rather than assumed either way.
- [x] Investigate wiring `npm run sync:ios` into an Xcode build phase so the bundled corpus resource can't silently drift from `corpus/quotes.json` — researched rather than guessed at (see [ios/README.md](ios/README.md)): a naive `preBuildScripts` Run Script phase on the app target almost certainly runs too late, since `ColdOpenCore` (the package that owns the resource) builds as a dependency, bundling its resources, before the app target's own build phases execute. The SPM-native fix — a build tool plugin — has a documented bug affecting exactly this case ([swift-package-manager#7120](https://github.com/apple/swift-package-manager/issues/7120), `Bundle.module` accessor not synthesized for plugin-generated resources). Decided to keep the manual `npm run sync:ios` step rather than ship an automation that likely doesn't work and can't be tested here.
- [ ] **(newly discovered)** If someone wants to revisit automating the iOS corpus sync: prototype an SPM build tool plugin, budgeting time to work around swift-package-manager#7120 (or route around it by having `QuoteCorpus.load()` accept an explicit file path instead of relying on `Bundle.module`).
- [x] No app icon or launch screen asset catalog existed — resolved: [ios/AppIcon.xcassets](ios/AppIcon.xcassets) now has a real `AppIcon.appiconset` (the "Cold Open Wipe + Starfield" concept, updated 2026-09-17 to add 3 pixel-tested star flecks on top of the original wipe — see the icon-concepts proposals — generated pixel-exact from the approved design via script, not hand-drawn), wired into both `ColdOpen` and `ColdOpenMac` targets in `project.yml`. JSON validated, YAML re-validated after the edit. **Not yet confirmed inside Xcode itself** — no Xcode here to open the asset catalog and check it renders. No launch screen beyond the existing empty `UILaunchScreen` dict.

**Android widget**

- [ ] Set up Android Studio project + Jetpack Glance widget module — **source scaffold written** ([android/](android/): Gradle version-catalog project, `:core` Kotlin/JVM module, `:app` module with `MainActivity` + `ColdOpenWidget`), **but not yet built, synced, or opened** — done from a Windows machine with no JDK/Android SDK/Gradle available. No Gradle wrapper is checked in either (deliberately — see android/README.md, it's a binary bootstrapper that shouldn't be hand-fabricated). Next: on a machine with the Android toolchain, run `gradle :core:test` first, then open in Android Studio (which will offer to generate the wrapper) and confirm `:app` builds. See [android/README.md](android/README.md).
- [x] Bundle the compiled corpus with the app — `app/build.gradle.kts` now registers a `syncCorpus` `Exec` task (runs `npm run sync:android`) depended on by `preBuild`, so it's no longer a manual step. **Wiring itself is unverified** — no Gradle here to confirm `preBuild` actually triggers it, or that `npm` resolves correctly from Android Studio's Gradle invocation on Windows (`npm.cmd` vs `npm`). Manual `npm run sync:android` remains the fallback if it doesn't fire. See [android/README.md](android/README.md).
- [x] Implement random, no-repeat-until-exhausted quote selection (shared logic/schema with iOS, platform-native implementation) — algorithm implemented and unit-tested in `:core`'s `QuoteSelector` ([android/core/src/main/kotlin/com/coldopen/core/QuoteSelector.kt](android/core/src/main/kotlin/com/coldopen/core/QuoteSelector.kt)), and now wired into `ColdOpenWidget.kt` with SharedPreferences-backed persistence (see the persistence task a few lines down) — it's actually used, not just tested in isolation
- [ ] Build small and medium widget size layouts — widget provider XML (`cold_open_widget_info.xml`) declares resizable min/max dimensions and a Glance composable renders the content; not yet visually verified on an emulator/device
- [x] Wire manual refresh via Glance action + widget update — added `RefreshAction : ActionCallback` to `ColdOpenWidget.kt`, whose `onAction` calls `ColdOpenWidget().update(context, glanceId)` (re-runs `provideGlance`/`loadRandomQuote`, no duplicated draw logic). Wired to a clickable "New quote" `Text` via `GlanceModifier.clickable(onClick = actionRunCallback<RefreshAction>())`, laid out in a `Row` with the attribution text using `defaultWeight()` so it sits at the trailing edge — the API shape (`ActionCallback`, `actionRunCallback<T>()`, `GlanceModifier.clickable`) was confirmed against the official [Glance user-interaction doc](https://developer.android.com/jetpack/compose/glance/user-interaction) via WebFetch, not just training knowledge. **Still unverified by an actual Kotlin/Gradle build** — no compiler here to confirm it compiles against the pinned Glance 1.1.1 version.
- [ ] Verify custom font renders correctly across target API levels (bitmap fallback if needed) — open, no custom font chosen yet
- [x] Persist `QuoteSelector`'s "already shown" state via SharedPreferences so the widget's random selection is actually no-repeat — done. `QuoteSelector` (still pure Kotlin, no Android dependency) now takes an `alreadyShown: Set<String>` constructor param and exposes `shownTexts`, keyed on `Quote.text` (the same uniqueness key the corpus validator already enforces — no separate id field exists at this layer). `ColdOpenWidget.loadRandomQuote` reads/writes that set via `SharedPreferences` (`cold_open_widget` / `shown_quote_texts`) around each `next()` call. 4 new unit tests added in `QuoteSelectorTest.kt` (shownTexts accumulation, resuming mid-pool, resuming fully-exhausted, refill clears shown) alongside the 3 existing ones — all traced by hand step-by-step since no Kotlin compiler exists here to actually run them (see Environment note). Also widened the corpus-load catch from `IOException` to `Exception` while touching this code, since a malformed asset throws `org.json.JSONException`, not `IOException` — the widget should never crash either way.
- [ ] **(newly discovered)** Wire `npm run sync:android` into a Gradle `preBuild` task so the bundled corpus asset can't silently drift from `corpus/quotes.json`.
- [x] No app icon existed — resolved: implemented as adaptive-icon vector drawables ([android/app/src/main/res/drawable/ic_launcher_background.xml](android/app/src/main/res/drawable/ic_launcher_background.xml), same "Cold Open Wipe + Starfield" design as iOS/Windows (updated 2026-09-17 to add 3 star flecks as circle paths), reproduced as flat paths — no PNG needed since minSdk 26 already requires adaptive icon support). `AndroidManifest.xml` now sets `android:icon`/`android:roundIcon`. XML validated with `ElementTree`. **Not yet confirmed inside Android Studio** — no Gradle/Android SDK here to actually render it.
- [ ] **(newly discovered)** The `androidx.glance.*` API calls in `ColdOpenWidget.kt` were written from training knowledge, not verified against the pinned Glance 1.1.1 docs or an actual build — expect import/signature fixes on first compile.

**Exit criteria:** Widget installable on iOS and Android, shows real random quotes from the actual corpus, manual refresh works, no crashes.

---

## M2 — Sharing

- [ ] Design the share card layout (themed background, quote in display face, attribution line, wordmark)
- [ ] Implement on-device share card rendering — iOS (Core Graphics)
- [ ] Implement on-device share card rendering — Android (Canvas)
- [ ] Export share cards at 1080×1920 (Stories/Reels) and 1080×1080 (feed)
- [ ] Wire iOS share sheet (`UIActivityViewController`) with pre-attached image
- [ ] Wire Android share intent (`ACTION_SEND`, image MIME type) with pre-attached image
- [ ] Integrate Instagram Stories sticker/background API where available
- [ ] Confirm fallback to generic OS share sheet for X, Facebook, Messages, WhatsApp
- [ ] Add attribution text to every generated share card (non-negotiable — see CLAUDE.md)

**Exit criteria:** A quote can go from widget tap to a posted Instagram/X/Facebook story in under 10 seconds, on both platforms.

---

## M3 — Polish & Scheduled Refresh

- [ ] Build the large widget size (quote + attribution + faction emblem) — iOS and Android
- [ ] Implement scheduled auto-refresh respecting each OS's widget timeline budget
- [ ] Add user-facing cadence setting (e.g., daily, every few hours)
- [ ] Implement favorite/pin action (long-press or in-app), excluded from randomize + auto-refresh pool
- [ ] Build the companion app quote browser (searchable by episode/arc)
- [ ] Instrument analytics for the D30 retention and share-rate metrics defined in the PRD
- [ ] Accessibility pass: VoiceOver/TalkBack reads quote + attribution as a single label

**Exit criteria:** D30 retention and share-rate metrics are actually being measured against the PRD §2 goals.

---

## M4 — macOS / Windows

- [ ] Port the WidgetKit target to macOS (native), reusing the iOS corpus and rendering code where possible — **source scaffold written**: `ios/project.yml` extended with `ColdOpenMac` + `ColdOpenWidgetExtensionMac` targets, both compiling the *same* `ios/ColdOpen`/`ios/ColdOpenWidget` source as the iOS targets (verified by inspection: no iOS-only APIs used) plus the shared `ColdOpenCore` package (already declares `.macOS(.v13)` support). **Not yet generated or built** — same environment constraint as the iOS scaffold (no macOS/Xcode/XcodeGen here). Next: on macOS, `xcodegen generate` and confirm all four targets (2 iOS + 2 macOS) coexist in one project without collision — untested project shape. See [ios/README.md](ios/README.md).
- [ ] Verify Notification Center + desktop widget placement on macOS — can't check without a Mac; open
- [x] No app icon for the macOS targets — resolved together with the iOS one: `ColdOpenMac` now also sets `ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon` and shares [ios/AppIcon.xcassets](ios/AppIcon.xcassets), which includes the full `mac` idiom size set (16/32/64/128/256/512/1024, 7 unique PNGs covering all 1x/2x combinations) alongside the iOS `universal` entry. Not yet confirmed inside Xcode.
- [ ] **(newly discovered)** `ENABLE_HARDENED_RUNTIME: true` was set on both macOS targets as a reasonable default for eventual notarization, but is unverified — confirm it doesn't cause build friction before code signing is actually set up.
- [x] Evaluate current maturity of the Windows Widgets Board API — done as research before scaffolding, not just assumed: fetched and read Microsoft's current "Implement a widget provider in a C# Windows App" doc first, since this API is niche enough that guessing from memory would've been unreliable. Findings: third-party widgets use `Microsoft.Windows.Widgets.Providers.IWidgetProvider`, implemented as an out-of-process COM server, with visual content as Adaptive Card JSON — only packaged (MSIX) apps can register as providers. Small/medium sizes only supported at this stage (no `large`, unlike iOS/Android P0 scope) — see PRD §4 vs. what Widgets Board's `Capability`/`Size` actually offers.
- [ ] Build the Windows widget target (small/medium/large parity with mobile P0 scope) — **source scaffold written** ([windows/](windows/): `WidgetProvider.Core` — `Quote`/`QuoteCorpus`/`QuoteSelector`/`QuoteCardTemplate`, unit-tested; `WidgetProvider` — `ColdOpenWidgetProvider` implementing `IWidgetProvider`, adapted from Microsoft's own sample code, including a working manual-refresh `Action.Execute`; `WidgetProvider.Package/Package.appxmanifest` — the extension declarations, adapted from the same doc). **Not yet built, packaged, or deployed** — no .NET SDK/MSBuild/Visual Studio on this machine. Widget only declares `small`/`medium` sizes so far (no `large` capability yet). See [windows/README.md](windows/README.md) for exactly what's doc-grounded vs. extrapolated.
- [ ] **(newly discovered)** No `.wapproj` packaging project exists — deliberately not hand-authored (see windows/README.md); must be created via Visual Studio's "Windows Application Packaging Project" template, then `WidgetProvider.Package/Package.appxmanifest`'s content merged in.
- [x] `QuoteSelector`'s in-memory pool didn't survive a widget provider process restart — resolved: `QuoteSelector` gained an `alreadyShown` constructor param and `ShownTexts` property (same shape as the Android fix, mirrored on purpose), keyed on `Quote.Text`. `ColdOpenWidgetProvider.UpdateWidget` writes the shown set into `WidgetUpdateRequestOptions.CustomState` (joined with `\u001F`, a control character real narration text won't contain) on every update, and the constructor seeds from any one running widget's `CustomState` on startup — `_selector` is one shared instance for the whole process, not per-widget, so any single widget's `CustomState` is a valid seed since they're all written identically. 4 new unit tests added in `QuoteSelectorTests.cs`, traced by hand (no .NET SDK here to actually run `dotnet test`). Caught and fixed a real bug mid-edit: an Edit call wrote a literal U+001F control byte into the C# source instead of the `\u001F` escape-sequence text — found by inspecting raw file bytes, fixed at the byte level, re-verified. **Known limitation, logged rather than solved:** `CustomState` stores full quote *text*, which will grow non-trivially once the ~130-quote corpus exists — a compact index-based encoding would need Windows to read `corpus/quotes.sqlite` instead of the flat JSON, which it doesn't yet (see the cross-platform SQLite task above).
- [x] No app icons existed for the Windows package — resolved: `Images\StoreLogo.png` (50x50), `Square150x150Logo.png`, `Square44x44Logo.png`, and `ProviderAssets\ColdOpen_Icon.png` (96x96) all now exist, same "Cold Open Wipe + Starfield" design as iOS/Android (updated 2026-09-17 to add 3 star flecks). **`ProviderAssets\ColdOpen_Screenshot.png` is still missing on purpose** — it's a screenshot of the widget's rendered content, not an icon, and needs either a real running widget or fabricated placeholder quote text to produce; neither belongs in an icon task. See `ProviderAssets/README.md`.
- [x] `Microsoft.WindowsAppSDK` NuGet version pinned in the `.csproj` files was a plausible-but-unverified guess (`1.6.240923002`) — checked NuGet.org directly rather than leaving it as a guess: current stable is `2.4.0` (2026-09-11), now pinned in [windows/WidgetProvider/WidgetProvider.csproj](windows/WidgetProvider/WidgetProvider.csproj). Still just a version number until an actual `dotnet restore` runs on a machine with the .NET SDK.
- [ ] **(newly discovered)** Add a `large` widget size `Capability` once the small/medium layouts are confirmed working, to reach parity with iOS/Android's P0 scope.
- [ ] **(newly discovered)** Generate `ProviderAssets\ColdOpen_Screenshot.png` once the widget can actually be run and screenshotted — don't fabricate placeholder quote text for it.
- [x] The Windows logo sizes were flagged as unverified — checked against Microsoft's current ["Construct your Windows App's Icon"](https://learn.microsoft.com/windows/apps/design/iconography/app-icon-construction) doc rather than left as a guess: base sizes (`StoreLogo` 50, `Square44x44Logo` 44, `Square150x150Logo` 150) were already correct, but the doc surfaced something not previously known — scale variants use a `<Name>.scale-<N>.png` filename convention (auto-discovered by Windows' resource system, no manifest change needed) rather than one bare file, matching what Visual Studio's own asset generator produces. Added the documented minimum (100/200/400%) for `Square44x44Logo`/`Square150x150Logo`; `StoreLogo` stays single-scale. See [windows/README.md](windows/README.md).
- [ ] **(newly discovered)** The full Microsoft Store-required icon asset set (AppList target-size icons at 14 sizes × 3 theme variants, tile assets, splash screen, `StoreLogo` scale variants) is not built — deliberately deferred until the distribution-plan decision (see Open items below) is actually made, since building it now would be speculative for a channel that might not be used.
- [ ] Confirm share-sheet equivalents work on both desktop platforms

**Exit criteria:** Feature parity with the mobile P0 scope (§4 of the PRD) on macOS and Windows.

---

## Backlog (P2 — not scheduled to a milestone yet)

- [ ] Faction-themed widget skins (Republic / Separatist / Jedi / Mandalorian palettes)
- [ ] Lock screen widget (iOS 16+ family, Android always-on-display equivalent)

## Open items blocking full completion (see PRD §11)

- [ ] Decide distribution plan (sideload/TestFlight vs. public app store listing) before investing further in store-specific polish — IP risk, needs explicit sign-off
- [ ] Spike real WidgetKit refresh-budget behavior on a physical device — confirms whether the <150ms refresh goal is realistic within OS constraints
