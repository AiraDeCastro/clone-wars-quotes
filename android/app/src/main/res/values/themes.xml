<resources>
    <style name="Theme.ColdOpen" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- ShareActivity renders a card and immediately hands off to the OS
         share sheet; this keeps it from flashing a blank window first. -->
    <style name="Theme.ColdOpen.Invisible" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
